"""Typed configuration package (settings + non-secret YAML defaults).

Kept at the repo root (not under ``src/``) so it can be imported as
``config.settings`` from scripts, tests, and the engine alike.
"""
